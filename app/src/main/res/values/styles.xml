<resources>


    <!-- Base application theme. -->
    <style name="BaseTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->

       <!-- <item name="android:background">@color/bg_color</item>-->
        <item name="android:textColor">@color/font_color</item>
        <item name="colorControlActivated">@color/base_color</item>
        <item name="colorControlHighlight">@color/base_color</item>

        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionBarTabTextStyle">@style/MyActionBarTitleText</item>

        <!-- Support library compatibility -->
       <!-- <item name="background">@color/bg_color</item>-->
        <item name="actionBarStyle">@style/MyActionBar</item>
        <item name="actionBarTabTextStyle">@style/MyActionBarTitleText</item>

    </style>


    <style name="AppTheme" parent="BaseTheme">
        <item name="android:background">@color/bg_color</item>

        <!-- Support library compatibility -->
        <item name="background">@color/bg_color</item>
    </style>


    <!-- ActionBar styles -->
    <style name="MyActionBar"
        parent="@style/Widget.AppCompat.ActionBar">
        <item name="android:background">@color/base_color</item>
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <!-- Support library compatibility -->
        <item name="background">@color/base_color</item>
        <item name="titleTextStyle">@style/MyActionBarTitleText</item>
        <item name="backgroundStacked">@color/base_color</item>
    </style>

    <!-- ActionBar title text -->
    <style name="MyActionBarTitleText"
        parent="@style/TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">@color/base_font_color</item>
    </style>

    <style name="BarTopTitle">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_gravity">left|center_vertical</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/base_font_color</item>
    </style>

</resources>
